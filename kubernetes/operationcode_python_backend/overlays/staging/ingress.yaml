apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  name: back-end
spec:
  rules:
  - host: api.dev.operationcode.org
    http:
      paths:
      - backend:
          serviceName: back-end-service
          servicePort: 80
        path: /
  tls:
  - hosts:
    - api.dev.operationcode.org
    secretName: back-end-tls
